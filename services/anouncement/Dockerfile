# Generated by Grok, created by xAI, Version: Grok (Fast setting), Date: 2025-10-05
FROM python:3.10-slim

WORKDIR /app

# Copy only this service's files and root requirements
COPY services/announcement/ .
COPY requirements.txt .

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Install TTS and news tools
RUN pip install --no-cache-dir TTS feedparser
RUN apt-get update && apt-get install -y ffmpeg && apt-get clean

# Expose port for FastAPI
EXPOSE 8005

# Run the service
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8005"]